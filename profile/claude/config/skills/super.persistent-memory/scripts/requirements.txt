# Python dependencies for the persistent-memory skill (pmem / memory.py).
#
# Single source of truth for the TESTED minimum versions. Install or upgrade with:
#   python3 -m pip install -r requirements.txt
#
# These are minimum bounds (>=), NOT exact pins. torch and transformers are pulled
# in transitively by sentence-transformers and are platform/CUDA-specific, so they
# are intentionally NOT listed here — let sentence-transformers resolve a build
# that matches the machine. check-pmem-deps.cjs reads these floors to decide
# whether to RECOMMEND an upgrade; it never installs anything on its own.

numpy>=1.26
sentence-transformers>=5.6.0

# Optional — enables the faster sqlite-vec semantic backend. Requires a Python
# built with loadable SQLite extension support; pmem falls back to a NumPy cosine
# path when it is absent, so this stays commented (not required to run).
# sqlite-vec>=0.1.6
